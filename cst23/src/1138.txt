/****************************************************************
*****************************************************************
*****************************************************************
*****************************************************************
*****************************************************************
*****************************************************************
****************************************************************/ 
#include <iostream>
using namespace std;

bool check(int b, int c)
{ 
     int a[8];     
     a[0]=120;
     a[1]=40;
     a[2]=85;
     a[3]=50;
     a[4]=100;
     a[5]=140;
     a[6]=70;
     a[7]=100;
    return b<=a[c-1];
}
bool noeql( int d , int e , int f , int g )
{
     return (((d-e)!=0)&& ((d-f)!=0)&&((d-g)!=0)&&((e-f)!=0)&&((e-g)!=0)&&((f-g)!=0)) ;
}
int main ()
{
    int na,nb,nc,nd,sum,rst=0;
    cin >> na >> nb >> nc >> nd; 
    int NA,NB,NC,ND;
    for(NA=1;NA<=8;NA++)
    for(NB=1;NB<=8;NB++)
    for(NC=1;NC<=8;NC++)
    for(ND=1;ND<=8;ND++)
    {
                      sum=check(na,NA)+check(nb,NB)+check(nc,NC)+check(nd,ND);
                       if((sum==4)&&noeql(NA,NB,NC,ND))
                       {
                                                       cout << NA << " " << NB << " " << NC << " " << ND <<endl;
                                                       rst=rst+1;
                       }
    }
    if (rst==0)
    cout<<-1<<endl;
    return 0;
}

